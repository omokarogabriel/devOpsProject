apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: project-5
  labels:
    app: database
    tier: database
spec:
  clusterIP: None
  selector:
    app: database
    tier: database
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
